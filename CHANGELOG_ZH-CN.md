# 更新日志

[English](CHANGELOG.md)

本模组的完整更新日志。最新版本默认展开，历史版本可点击展开查看。

---

<details open>
<summary><strong>v1.1.1</strong> — 配置自动迁移、许可证修复、包名重命名与质量改进</summary>

### 新增
- **配置自动迁移**：升级时模组会在首次启动自动检测旧配置版本并迁移，再也不用手动删除配置文件夹了！新增 `[meta]` 分组下的 `configVersion` 字段（仅内部使用，请勿修改）。进入世界后聊天框会提示迁移完成。
- **配置版本多语言支持**：新增的 `configVersion` 配置项及其悬停提示在全部20种语言中都有翻译，通过配置界面模组（Configured、Cloth Config 等）查看时会随游戏语言切换。

### 变更
- **包名规范化**：从 `com.example.autowindowsize`（Forge MDK 模板残留）改为 `com.jujumlqwq.autowindowsize`。所有 Java 源文件、`gradle.properties`（`mod_group_id`）和编译后的 class 路径均已更新。
- **修正作者信息**：所有地方（`mods.toml` 描述、全部20个关于页面文本文件）的作者名统一为 `JujuMLQwQ, ML`。（`mods.toml` 的 `authors` 字段和 `gradle.properties` 的 `mod_authors` 之前已经是正确的。）
- **更新联系邮箱**：README 和 WIKI（中英文）中的邮箱从 `3270728922@qq.com` 改为 `jujuawa@qq.com`。
- **更新 README 描述**：简介现在包含所有主要功能（无边框、三档窗口置顶、窗口状态循环、5个快捷键、60个预设、启动自动全屏/最大化/无边框）。
- **README 配置表格**：新增 `configVersion` 行和自动迁移说明。
- **README "最新版本"部分**：v1.1.1 现在是最新版本；v1.1.0 移到"上一个版本"。
- **WIKI 升级说明**：中英文 WIKI 中的"手动删除配置文件夹"已替换为"配置自动迁移"。

### 修复
- **许可证文件**：根目录 `LICENSE.txt` 之前是 Forge MDK 的 LGPL-2.1 模板（27KB），与 `mods.toml` 和 `gradle.properties` 中声明的 `license="MIT"` 矛盾。已替换为标准 MIT 许可证文本（1KB），版权归 JujuMLQwQ。GitHub 现在能正确识别许可证了。
- **Issue 模板死链接**：`.github/ISSUE_TEMPLATE/config.yml` 中的"常见问题"链接之前指向 `#常见问题`（README 中不存在的锚点），已改为实际存在的 `#notes` 章节。
- **1.0.9 之前旧配置检测**：如果存在旧的 `config/autowindowsize-client.toml` 文件，现在会在日志中输出警告（之前会被静默忽略）。

### 备注
- 这是一个以质量改进为主的版本，没有新增或移除任何面向玩家的功能。
- Mod ID 仍为 `autowindowsize`（未变），因此配置文件路径、语言文件路径和指令前缀（`/aws`）全部保持不变。
- 完全向后兼容：现有配置会在首次启动时自动迁移。

</details>

<details>
<summary><strong>v1.1.0</strong> — 无边框模式、窗口置顶、快捷键、关于页面与大规模界面重构</summary>

### 新增
- **无边框模式**：去除窗口标题栏和边框（GLFW_DECORATED = false）。支持三种状态：窗口化无边框（简单实现）、最大化无边框、全屏无边框（伪全屏 — 以屏幕分辨率运行的无边框窗口）。新增 `borderless` 配置项和 `/aws borderless` 指令。
- **启动自动无边框**：新增 `autoBorderless` 配置项（默认关闭）。开启后下次启动自动进入无边框模式。通过 `/aws borderless auto` 切换。
- **窗口置顶（3种模式）**：新增 `alwaysOnTopMode` 配置项（0=关闭，1=普通，2=强制）。普通模式使用 GLFW_FLOATING。强制模式通过每帧 `glfwFocusWindow` 加焦点丢失回调双重保险重新抢焦点，可覆盖其他置顶窗口（如教室监控软件）。通过 `/aws top` 切换。
- **循环切换窗口状态按钮**：设置界面中一键在 窗口化 → 最大化 → 全屏 之间循环切换。也可通过快捷键触发。
- **5个快捷键**（默认未绑定，在 选项 → 控制 → Auto Window Size 中设置）：
  - 打开窗口设置
  - 居中窗口
  - 切换无边框
  - 循环切换窗口状态
  - 切换窗口置顶
  - 所有快捷键按下时都会在聊天框给出反馈。
- **关于页面**：游戏内信息页面（通过设置中的"关于"按钮进入），包含6个模块 — 制作目的、MOD介绍、使用方法、下载渠道、反馈渠道、作者信息。通过按语言分类的 `.txt` 文件支持20种语言。
- **调试模式**：新增 `debug` 配置项。开启后将详细诊断信息（预设可用性检查、无边框状态切换、窗口状态变化等）输出到游戏日志。通过 `/aws debug` 切换。
- **新增指令**：`/aws gui`（打开设置）、`/aws status`（显示当前状态）、`/aws toggle`（切换锁定）、`/aws top`（循环切换置顶）、`/aws debug`（切换调试）、`/aws borderless`（切换无边框，含 `auto` 子指令）。共计15个指令。
- **帮助分页**：`/aws help [页码]` 现在支持2页（每页8条指令），标题行和页码提示常驻显示。
- **自定义分辨率输入实时校验**：在自定义宽/高输入框中输入时，如果输入非法（非数字或超出范围），应用按钮会被禁用。鼠标悬停在禁用的应用按钮上会显示提示，说明合法范围和超出了哪个边界。
- **预设自动禁用增强**：预设现在会在以下情况被禁用：(1) 超过屏幕分辨率，(2) 低于配置最小值，(3) 低于硬编码最小值（856×482），(4) 匹配当前窗口大小。

### 变更
- **设置入口确认在辅助功能设置**："窗口设置"按钮位于 选项 → 辅助功能设置（v1.0.9从视频设置迁移）。这是永久位置，选择此位置是为了兼容 Embeddium 等完全替换视频设置界面的模组。
- **设置界面布局重构**：固定窗口大小改为大按钮；"记住窗口状态"和"居中窗口"并排；"调试"和"关于"在底部并排。比例按钮循环切换比例并显示当前分辨率。
- **信息区最终定稿**：4行 — (1) 屏幕分辨率 + 居中指示器，(2) 窗口状态 + 配置文件状态（常驻显示），(3) 状态详细说明，(4) 自定义输入边界提示（在输入框下方）。
- **滚动系统重建**：设置界面现在继承 `AbstractSelectionList` 实现原生Minecraft滚动列表，标题和完成按钮有独立的不透明背景（与原版选项界面完全一致）。滚动条位置与原版一致。
- **配置注释重构**：简化每个选项的注释，删除重复的Range行（Forge会自动生成），统一使用英文注释。
- **语言数量**：完整支持20种语言（en_us, zh_cn, zh_tw, ja_jp, ko_kr, ru_ru, fr_fr, de_de, es_es, pt_br, it_it, tr_tr, nl_nl, pl_pl, vi_vn, ar_sa, th_th, sv_se, cs_cz, id_id）。

### 修复
- 修复设置界面按钮布局与标题和完成按钮重叠的问题（用AbstractSelectionList重建）。
- 修复滚动条位置与按钮重叠的问题（现在与原版完全一致）。
- 修复按钮选中/高亮边框超出按钮边界的问题（重写了`isSelectedItem()`）。
- 修复比例按钮在切换最大化/全屏后错误显示"自定义"的问题。
- 修复点击预设后比例按钮回弹到上一个比例的问题（状态同步问题）。
- 修复自定义分辨率输入框在拖动窗口调整大小时不实时更新的问题。
- 修复从预设切换到自定义时输入框显示上一个值的问题。
- 修复固定窗口大小未禁用分辨率预设按钮的问题（只禁用了比例按钮）。
- 修复无边框模式下窗口状态检测错误的问题（伪全屏被错误检测为最大化）。
- 修复无边框模式下从全屏切换到最大化时顶部出现空白的问题。
- 修复无边框模式下快速切换状态后窗口变成全屏分辨率的窗口化状态的问题。
- 修复强制置顶模式下点击其他窗口标题栏后丢失焦点的问题（添加了焦点丢失回调）。
- 修复点击某些按钮后滚动位置重置到顶部的问题（通过`lastScroll`保持滚动位置）。
- 修复切换到预设较少的比例时滚动位置超出边界的问题。
- 修复`window.json`首次启动时不生成的问题（将确保检查移到`onTick`中，用一次性标志位控制）。
- 修复进入全屏时重复发送聊天消息的问题（删除了冗余的"锁定已禁用"消息，只显示状态切换消息）。
- 修复配置界面中Debug配置项缺少翻译的问题（在全部20种语言中添加了`config.autowindowsize.window.debug`和`.tooltip`键）。
- 修复鼠标悬停在输入框或按钮上时无法滚轮滚动的问题。
- 修复各种细微的UI对齐和间距问题。

</details>

<details>
<summary><strong>v1.0.9</strong> — 分辨率预设扩充、窗口位置记忆、配置重构与界面打磨</summary>

### 新增
- **分辨率预设扩充**：5种比例（16:9 / 16:10 / 4:3 / 5:4 / 21:9）每种12个预设（3排），点一下立即切换到对应分辨率并居中。
- **自定义分辨率输入**：比例切到"自定义"时出现宽+高输入框和应用按钮，带实时边界校验；输入超出合法范围时自动钳制到最近的合法值并给出警告提示。
- **启动延迟可配置**：配置文件新增 `startupDelay`（0.5~10.0秒，默认1.5秒），控制启动后等待多久再应用窗口大小和位置。
- **窗口位置记忆**：配置文件新增 `rememberPosition`（默认关闭），开启后退出时保存窗口位置与大小，下次启动恢复（而非强制居中）；与自动全屏/最大化兼容：启动时先全屏/最大化，退出后恢复记忆位置。
- **配置文件统一目录**：所有配置文件迁移到 `config/AutoWindowSize/` 目录下（主配置 config.toml + 窗口状态 window.json），保持 config 文件夹整洁。
- **配置界面多语言**：Forge 配置值加 `.translation()` 翻译键，通过配置界面模组打开时配置名称和描述随游戏语言切换，20种语言全部翻译。
- **新增5个指令**：`/aws help`（指令列表，置顶）、`/aws fullscreen`（切换全屏）、`/aws maximize`（切换最大化）、`/aws remember`（切换位置记忆）、`/aws resolution`（设置分辨率，支持"比例 预设"和自定义"宽 高"两种格式，带 Tab 补全）。
- **按钮悬停提示**：窗口设置界面所有按钮新增鼠标悬停描述（tooltip），支持换行。

### 调整
- **设置入口迁移**：从「选项 → 视频设置」迁移到「选项 → 辅助功能设置」（目的：兼容 Embeddium 等完全替换视频设置界面的模组，避免"窗口设置"按钮消失）。
- **设置界面信息区重构**：从两行扩展为四行：（1）屏幕分辨率+居中标记，（2）窗口状态+配置文件状态（常驻），（3）当前状态的详细说明，（4）自定义输入框下方的边界值+输入要求（常驻）。
- **配置文件注释重构**：精简每项注释、去掉重复的 Range（Forge 自动生成）、全英文、加段分隔线。
- **指令顺序重排**：help → gui → status → toggle → lock → unlock → fixed → center → fullscreen → maximize → remember → resolution。
- **语言从15种扩展到20种**：新增阿拉伯语、泰语、瑞典语、捷克语、印尼语。

### 修复
- 修复比例按钮在最大化/全屏切换后错误显示"自定义"的问题。
- 修复点击分辨率预设后比例显示滞后/回弹的问题。
- 修复自定义分辨率输入框不随窗口拖动实时更新的问题。
- 修复固定窗口大小开启后未禁用比例按钮和预设按钮的问题。
- 修复应用按钮和输入框点击后高亮背景过大溢出按钮边界的问题。
- 修复辅助功能设置界面按钮在无对应语言文件时位置偏移/变长的问题。
- 修复设置界面滚动条与按钮重叠的问题，调整到与原版滚动条位置一致。
- 修复自动全屏启动后取消全屏不居中的问题。
- 修复设置界面中多处按钮间距、对齐和布局细节问题。

### 注意
- **配置迁移**：从 1.0.9 之前版本升级时，请手动删除旧的 `config/autowindowsize-client.toml` 和新的 `config/AutoWindowSize/` 文件夹（如果存在），然后启动游戏自动生成全新配置文件。
- 启动自动全屏和窗口位置记忆功能为独立实现，灵感来自 [Window Control](https://www.curseforge.com/minecraft/mc-mods/window-control)，未复制任何代码。

</details>

<details>
<summary><strong>v1.0.8</strong> — 常用分辨率预设 + 自定义分辨率 + 设置界面重构</summary>

### 新增
- 窗口设置界面新增"分辨率预设"：按 16:9 / 16:10 / 4:3 / 5:4 / 21:9 五种比例分组，点一下立即切换到对应分辨率并居中。
- 新增"自定义分辨率"：比例切到最后一项时出现输入框，可手动输入宽和高，点应用立即切换。
- 已匹配当前分辨率的预设按钮自动禁用，避免重复操作。
- 分辨率超过当前屏幕时对应按钮自动禁用。
- 实时检测屏幕分辨率变化，玩家在系统里改了屏幕分辨率后设置界面自动跟上。
- 固定窗口大小开启后，比例切换、预设按钮、自定义输入框和应用按钮全部自动禁用。
- 语言支持扩展到 15 种：简体中文、繁體中文、English、Русский、Français、日本語、한국어、Deutsch、Español、Português (BR)、Italiano、Türkçe、Nederlands、Polski、Tiếng Việt。
- 预设扩充：16:9 加 1024×576 / 1152×648 / 3840×2160；16:10 加 2560×1600；4:3 加 640×480 / 1152×864 / 1920×1440。

### 调整
- 设置界面重构为原版滚动列表（AbstractSelectionList），自带滚动条、上下渐隐和鼠标滚轮滚动，观感和原版视频设置一致。
- 比例按钮实时显示当前比例和窗口分辨率，不再显示"下一个比例"。
- 拖动窗口后若分辨率不匹配当前比例，自动切到"自定义"；最大化和全屏状态下不自动切。
- 拖动后分辨率刷新延迟从 0.5 秒缩短到 0.2 秒。
- 标题下移到原版标准位置。
- 全屏提示文字优化："⚠ 全屏模式下锁定和固定等功能暂时停用，退出全屏后自动恢复"。

### 修复
- 修复 1.20.1 下使用 `InputConstants.UNKNOWN` 导致启动崩溃的问题（改用 `-1` 表示未绑定按键）。
- 修复切换到无翻译的语言时视频设置里"窗口设置"按钮位置偏移的问题。
- 修复开启固定窗口大小后预设按钮仍可点击的问题。
- 修复切换到"自定义"时输入框值不更新为当前分辨率的问题。

</details>

<details>
<summary><strong>v1.0.7</strong> — 新增启动自动全屏 / 自动最大化</summary>

### 新增
- 新增"下次启动自动全屏"：窗口设置界面新增一个持久开关，勾选后**下一次启动**游戏自动进入全屏；它不改变当前这次窗口，效果在下次启动时体现，选择会写回配置文件并一直保持。
- 新增"下次启动自动最大化"：与自动全屏配对的持久开关，下次启动自动把窗口最大化；两者**互斥**——界面里开启其中一个会自动关掉另一个并灰掉提示，配置里两个引导目标同时为 true 时本次都不生效并自动写回 false。
- 新增整合包作者一次性首次引导：配置文件 `[startup]` 段新增 `applyStartupGuide`（默认 `false`）、`startFullscreen` 与 `startMaximized`。作者把 `applyStartupGuide` 设为 `true` 并随整合包发布后，玩家首次启动会按引导目标自动全屏或最大化一次，同时把玩家对应的游戏内偏好同步过去，随后 `applyStartupGuide` 自动写回 `false`，之后只听玩家自己的设置。
- 启动自动全屏时跳过"设配置分辨率并居中"直接全屏；启动自动最大化时直接最大化，并复用加载期最大化的恢复链，用户取消最大化后自动回到配置尺寸并居中；进/退全屏后的锁定与固定状态仍由既有全屏逻辑统一处理。

### 调整 / 修复
- 更正苦力怕论坛（KLBBS）发布链接为 https://klpbbs.com/thread-173769-1-1.html 。
- 文档结构调整：README 精简为一屏简介，详细内容迁移到 WIKI；新增英文/中文 Wiki 文件。
- 修复窗口设置界面按钮与分辨率文字、禁用提示相互重叠的问题；重排设置界面布局：按钮列统一间距顺次排列、分辨率信息自动跟随按钮列、"完成"按钮固定于底部，后续新增选项不再容易挤位。
- 按钮文案精简为"功能：开/关"，去掉多余的"（点击开启/关闭）"，并为每个按钮新增鼠标悬停说明（tooltip）。
- 配置文件注释重写：去掉 `====` 装饰线，改为上英文下中文的简洁双语注释，选项之间用一行 `##` 分隔。
- 修复"启动自动全屏后按 F11 退出全屏窗口不居中"：本次为自动全屏启动时，退出全屏后自动回到配置分辨率并居中；游戏中手动 F11 进出仍恢复原位置。
- 修复手动把 `autoFullscreen` 与 `autoMaximized` 都改为 true 时未自动归零的问题：启动时检测到双 true 会自动把两者写回 false，局内两个按钮不再互相禁用。
- 一次性引导现在会让位已有玩家偏好：若玩家已开启任一启动偏好（`autoFullscreen` / `autoMaximized`），整合包一次性引导不再覆盖玩家选择，并自动把 `applyStartupGuide`、`startFullscreen`、`startMaximized` 全部写回 false。

### 说明
- 默认安全：`applyStartupGuide` 默认 `false`，玩家单独安装本模组时不会被强制全屏，行为与旧版一致。
- 作者工作流：自己开发机上测试时启动一次后 `applyStartupGuide` 会变回 `false`，打包发布整合包前请把它改回 `true`。
- 启动自动全屏的设计参考了模组 [Window Control](https://www.curseforge.com/minecraft/mc-mods/window-control) 在启动时进入指定窗口模式的思路（本模组为独立实现，不包含其代码）。

</details>

<details>
<summary><strong>v1.0.6</strong> — 新增固定窗口大小功能</summary>

### 新增
- 新增"固定窗口大小"开关（窗口设置界面按钮 + `/aws fixed` 指令）：开启后把窗口**当前大小**完全锁死（最小=最大=当前值）。它不会跳变到配置分辨率、也不会强制居中——只是不能再改大小，并且标题栏最大化按钮会在系统层被禁用（GLFW_RESIZABLE=false，Windows 上移除 WS_MAXIMIZEBOX），从根上杜绝"假最大化"
- 窗口最大化或全屏时，固定窗口大小开关本身也会禁用（并给出提示），而不是去锁最大化时铺满屏幕的尺寸
- 全屏仍然可用（GLFW 全屏不受窗口尺寸限制约束），退出全屏后自动回到固定尺寸
- 固定窗口大小只受全屏/最大化禁用（临时禁用、退出恢复）；"配置分辨率过低"状态只禁用最小尺寸锁定，不影响固定窗口大小与居中（它们不依赖配置值）

### 说明
- 固定窗口大小为运行时开关（与最小尺寸锁定一致），默认关闭，重启游戏后恢复默认；固定开启时再点锁定按钮不产生额外效果（固定优先）
- 灵感来源：固定窗口大小的想法参考了模组 [Locked Window Size](https://modrinth.com/mod/locked-window-size)（LGPL-3.0 协议）。本模组为独立实现，仅使用 GLFW 官方公开 API，未复制其任何源代码，因此仍以 MIT 协议开源。

</details>

<details>
<summary><strong>v1.0.5</strong> — 修复加载期间全屏/最大化相关的窗口状态</summary>

### 修复
- 修复在游戏加载界面期间手动全屏或最大化窗口，进入主菜单后仍被模组强制改回配置分辨率的问题：现在启动初始化会检测窗口状态，若玩家已主动全屏或最大化，则先不强制改动窗口，记为"待初始化"。
- 进一步修复：加载期间最大化后取消最大化，窗口会停在系统默认大小而非配置分辨率；加载期间进入全屏、退出后窗口没有居中。现在退出全屏或取消最大化时，会自动把窗口设回配置分辨率并居中。
- 再修复：加载期间先最大化、再进入全屏，退出全屏后会进入"假最大化"（标题栏还原按钮还在、实际尺寸却是配置值）。现在这种情况退出全屏后会真正恢复最大化状态。
- 再修复：承接上一条，恢复最大化后再取消最大化，窗口大小正常但没有居中；现在这条完整链结束后会补一次居中，且不影响游戏中正常取消最大化（仍恢复原位）。

</details>

<details>
<summary><strong>v1.0.4</strong> — 配置体验优化、发布页信息补全与启动时机调整</summary>

### 新增 / 优化
- 配置文件注释采用"上英文、下中文"的简洁排版，Range 由 Forge 自动生成、不重复书写
- 配置取值范围改为以代码硬编码最小值为下限（宽 856、高 482），而非原先的 1
- Mods 列表（mods.toml）描述重写为分段、详细的版式，并补充 CurseForge / Modrinth / MC百科 / BBSMC 官方页面链接
- 支持 Better ModList：详情右侧显示蓝色可点击链接与"客户端"徽标；原版模组列表中链接亦可点击
- 窗口设置界面在全屏时，分辨率以黄色并标注"（全屏）"，一眼区分窗口/全屏状态
- 新增"居中窗口"按钮：窗口化时一键在当前显示器居中；全屏、最大化、最小化、已居中时自动禁用并显示原因；新增 `/aws center` 指令
- 文档新增"不兼容手机版"提示、Mod 核心目的与未来规划说明

### 调整
- 窗口大小生效时机由启动后约 2 秒提前为约 1.5 秒（30 tick）

### 修复
- 修复配置文件可被设置为低于代码硬写死分辨率的 bug：硬最小值常量统一收敛到 Config，配置层下限与运行时锁定下限指向同一常量，不再脱节
- 修复修改窗口尺寸后立即进入全屏时，"窗口尺寸变化中…"提示常驻不消失、UI 卡住的问题（全屏期间直接对齐当前尺寸、不进拖动提示；窗口模式下拖动时数字冻结、停止 0.5 秒后一次性刷新）

</details>

<details>
<summary><strong>v1.0.3</strong> — 延迟初始化 & 全屏提示优化</summary>

### 新增
- 窗口大小在游戏启动2秒后设置，避免加载界面拉伸，低配电脑也够用
- 全屏禁用提示现在会根据全屏前状态显示不同文字（退出后恢复开启 / 保持关闭）
- 配置文件注释中英双语

### 修复
- 第一次启动的无障碍引导界面不再跳过窗口初始化

</details>

<details>
<summary><strong>v1.0.2</strong> — 指令系统、全屏支持与界面优化</summary>

### 新增
- `/aws` 指令系统：`toggle`、`lock`、`unlock`、`status`、`gui`
- 全屏自动禁用：进入全屏时临时禁用锁定，退出全屏后按全屏前状态自动恢复
- 窗口设置界面按钮状态实时同步，无需重开界面
- 锁定禁用时显示红色原因文字（分辨率过低或全屏模式）
- `/aws status` 指令显示当前屏幕分辨率
- Mods 界面添加 GitHub 和 Issues 链接

### 调整
- 键位功能改为打开窗口设置界面（原为切换锁定）
- 硬编码最小分辨率从 1024×576 降为 856×482，兼容更多老显示器
- 退出全屏不再强制设置窗口大小，由 Minecraft 自动恢复全屏前状态
- 「游戏分辨率」改为「当前游戏窗口分辨率」，避免和 MC 自带「全屏分辨率」混淆
- 优化聊天提示文字样式，使用 ✔/✘/ℹ/● 符号区分不同类型消息
- 作者：JujuMLQwQ、ML

### 修复
- 修复 ConfigScreen 最小化再恢复后"1秒刷新提示"常驻的问题
- 全屏检测改用 GLFW 原生 API，更可靠
- 按钮宽度与视频设置原生按钮完全一致

</details>

<details>
<summary><strong>v1.0.1</strong> — 视频设置原生入口 & 窗口设置界面</summary>

### 新增
- 视频设置界面原生「窗口设置」按钮入口（位于全屏分辨率下方）
- 全新窗口设置界面，可手动开关锁定
- 实时显示当前屏幕分辨率和游戏窗口分辨率
- 进入世界时聊天栏提示当前锁定状态
- 拖动窗口后 1 秒自动刷新分辨率显示（带拖动中提示）

### 调整
- 键位默认改为未绑定，玩家自行在控制设置中绑定
- 默认分辨率从 1440×810 改为 1280×720，兼容更多老显示器
- 优化聊天栏提示文本样式，使用更专业的颜色和格式

### 修复
- 重写全屏逻辑：配置分辨率高于屏幕分辨率时，自动禁用锁定且不改动窗口大小，避免显示 bug
- 修复按钮在不同 GUI 缩放下的布局问题，改为原生插入列表
- 移除"分辨率过低时退出游戏"的设计，改为聊天栏提示
- 锁定禁用时，退出全屏不再改动窗口大小

</details>

<details>
<summary><strong>v1.0.0</strong> — 初始版本</summary>

- 初始版本
- 启动自动设置窗口大小并居中
- 锁定最小窗口尺寸
- 自定义按键切换锁定
- 多显示器支持

</details>
