# Auto Window Size

> Minecraft Forge 1.20.1 客户端模组 | 自动窗口大小与锁定

---

## 制作目的

在制作整合包时，我经常遇到这样的问题：好不容易把各个模组的 UI 界面布局调试好了，结果玩家换了一个分辨率启动游戏，窗口大小变了，所有 UI 布局全乱了。

为了解决这个问题，我做了这个模组：启动时自动把窗口设置为指定大小并居中，同时锁定最小窗口尺寸，确保整合包的 UI 布局在不同玩家的电脑上都能保持一致。

---

## 功能特性

- **启动自动设置窗口大小**：启动游戏时自动将窗口设置为配置的分辨率，并在屏幕上居中
- **锁定最小窗口尺寸**：锁定后窗口只能放大，不能缩小到配置尺寸以下，保证 UI 布局不被破坏
- **视频设置原生入口**：在「选项 → 视频设置」中原生插入「窗口设置」按钮，位于「全屏分辨率」下方，随列表滚动和 GUI 缩放正常布局
- **自定义按键切换**：可在控制设置中绑定按键，快速切换锁定状态（默认未绑定）
- **分辨率过低自动禁用**：当配置分辨率或最小分辨率高于玩家屏幕分辨率时，自动禁用锁定功能且不改动窗口大小，避免出现显示 bug
- **退出全屏恢复**：从全屏切换回窗口模式时，自动恢复配置的窗口大小并居中
- **进游戏状态提示**：每次进入世界时，聊天栏提示当前锁定状态（已锁定 / 已解锁 / 已禁用）
- **实时分辨率显示**：窗口设置界面实时显示当前屏幕分辨率和游戏窗口分辨率，拖动窗口后 1 秒自动刷新
- **多显示器支持**：自动检测游戏窗口所在的显示器，在对应显示器上居中

---

## 安装方法

1. 确保已安装 Minecraft 1.20.1 和 Forge 47.x
2. 将模组 jar 文件放入 `.minecraft/mods` 文件夹
3. 启动游戏即可

---

## 配置文件

配置文件路径：`.minecraft/config/autowindowsize-client.toml`

```toml
[window]
    # 启动窗口宽度，也是锁定时的最小宽度
    # 范围：1 ~ 7680
    width = 1280

    # 启动窗口高度，也是锁定时的最小高度
    # 范围：1 ~ 4320
    height = 720
```

> 注意：代码中硬编码了最小分辨率 1024×576，配置值低于此值时仍会以 1024×576 作为最小锁定尺寸。

---

## 使用说明

### 打开窗口设置

1. 主菜单：`选项...` → `视频设置...` → 点击 `窗口设置` 按钮
2. 游戏中：`Esc` → `选项...` → `视频设置...` → 点击 `窗口设置` 按钮

### 切换锁定

- **界面切换**：在窗口设置界面点击「窗口锁定」按钮
- **按键切换**：在控制设置中绑定「切换窗口大小锁定」按键，进游戏后按该键切换

### 锁定状态说明

| 状态 | 说明 |
|------|------|
| 已锁定 | 窗口最小尺寸被锁定，只能放大不能缩小 |
| 已解锁 | 窗口大小可自由调整 |
| 已禁用 | 配置分辨率高于屏幕分辨率，锁定功能自动禁用，窗口大小保持不变 |

---

## 常见问题

**Q：为什么我改了配置文件的分辨率，启动后窗口没变？**
A：如果配置的分辨率高于你的屏幕分辨率，模组会自动禁用锁定且不改动窗口大小。请将配置值改为小于等于屏幕分辨率的值。

**Q：锁定后窗口还能放大吗？**
A：可以。锁定只限制最小尺寸，不限制最大尺寸，你可以随意放大窗口。

**Q：怎么自定义切换锁定的按键？**
A：`选项...` → `控制...` → 搜索「自动窗口大小」→ 点击「切换窗口大小锁定」→ 按下你想绑定的键。

**Q：支持多显示器吗？**
A：支持。模组会自动检测游戏窗口所在的显示器，并在对应显示器上居中。

---

## 技术信息

- **Minecraft 版本**：1.20.1
- **Forge 版本**：47.x
- **Mod ID**：`autowindowsize`
- **开源协议**：MIT

---

## 更新日志

### v1.0.1
- 按钮入口改为视频设置界面原生插入（位于全屏分辨率下方）
- 新增窗口设置界面，可手动开关锁定
- 新增实时显示屏幕分辨率和游戏分辨率
- 新增进游戏聊天栏提示锁定状态
- 键位默认改为未绑定，玩家自行设置
- 默认分辨率改为 1280×720
- 重写全屏逻辑：配置分辨率过高时自动禁用锁定且不改动窗口
- 优化提示文本样式

### v1.0.0
- 初始版本
- 启动自动设置窗口大小并居中
- 锁定最小窗口尺寸
- 自定义按键切换锁定
- 多显示器支持

---

## 致谢

感谢所有使用本模组的玩家和整合包制作者。
反馈与建议欢迎提交 Issue。
